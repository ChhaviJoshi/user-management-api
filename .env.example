# Server Configuration
PORT=3000

# Database Configuration
# The user should replace these with their own local PostgreSQL details
DB_USER=postgres
DB_HOST=localhost
DB_NAME=user_management_db
DB_PASS=UPDATE_THIS_WITH_YOUR_PASSWORD
DB_PORT=5432

# Security Keys
# The user should enter any random string here
JWT_SECRET=change_this_to_a_secret_key